package main.control;

/**
 * This interface does all chores to interact with user.
 * Such as operation IO, calling model for calculations and etc.
 *
 */
public interface Controller {
	
	/**
	 * used to initialize the status for models.
	 */
	void init();
}
